Reliable packet tests generate deterministic audio/container fixtures in a
per-test temporary directory at runtime. This resource directory is processed so
future binary fixtures can be added without changing Package.swift again.
